{% extends 'base.html.twig' %}

{% block title %}LE TITRE EST ICI!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    {# <h1>Hello {{ prenom }}! ✅</h1> #}

    <h1>Hello {{ prenom | upper }}! ✅</h1> {# Mise en majuscule#}


    {#<h1>Hello {{ prenom | upper | lower }}! ✅</h1> Mise en minuscule

    {% if departement == 35 %}
        <p>Departement : {{ departement }}, Vous êtes sur Rennes</p>
    {% else %}
        {% for i in 1..20 %}
            <div> nombre {{i}} </div>
        {% endfor %}
    {% endif %}

    {% for i in pays  %}
        <div> Pays : {{i}} </div>
    {% endfor %}

    {% for i in pays  %}
        <div> Pays : {{i.nom}} </div>
    {% endfor %}

    <div> Ville : {{bien.ville}} </div>
    #}

   <h2>Nos biens à la vente</h2>
   <p>{{bien1.ville}} prix :<i>{{bien1.prix}} </i></p>
   <p>{{bien2.ville}} prix :<i>{{bien2.prix}} </i></p>

</div>
{% endblock %}